<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fx.shop.mapper.ComAreaMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.fx.shop.entity.ComArea">
        <id column="id" property="id" />
        <result column="grade" property="grade" />
        <result column="code" property="code" />
        <result column="name" property="name" />
        <result column="parent_code" property="parentCode" />
        <result column="zip_code" property="zipCode" />
        <result column="area_code" property="areaCode" />
        <result column="short_name" property="shortName" />
        <result column="pinyin" property="pinyin" />
        <result column="longitude" property="longitude" />
        <result column="latitude" property="latitude" />
        <result column="merger_name" property="mergerName" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, grade, code, name, parent_code, zip_code, area_code, short_name, pinyin, longitude, latitude, merger_name
    </sql>

</mapper>
