<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fx.shop.mapper.OrderDetailMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.fx.shop.entity.OrderDetail">
        <id column="id" property="id" />
        <result column="order_id" property="orderId" />
        <result column="order_no" property="orderNo" />
        <result column="product_id" property="productId" />
        <result column="product_name" property="productName" />
        <result column="product_img" property="productImg" />
        <result column="price" property="price" />
        <result column="product_count" property="productCount" />
        <result column="sub_total_amount" property="subTotalAmount" />
        <result column="sub_total_weight" property="subTotalWeight" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, order_id, order_no, product_id, product_name, product_img, price, product_count, sub_total_amount, sub_total_weight, create_time, update_time
    </sql>

</mapper>
